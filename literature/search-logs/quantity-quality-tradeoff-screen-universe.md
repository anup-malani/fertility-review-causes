# C.3.d screen universe

Generated by `source/build/goldset/353_c3d_screen_universe.py`. Do not edit by hand.

**3254 records** across 4 screened arms.

## Per arm, before deduplication

An arm whose records all arrive via another arm is REDUNDANT, which is a different finding from EMPTY. Counting only after the dedup hides it (`dedup-before-counting-hides-redundant-rung`).

| arm | declared frame | pulled | new after dedup |
|---|---|---|---|
| `theory` | 838 | 838 | 838 |
| `forward-shock` | 699 | 698 | 686 |
| `forward-design` | 500 | 500 | 472 |
| `backward` | 1006 | 1007 | 928 |

## Injection

A query-built universe omits the hand-sourced records that defined it. How many anchors were **already present** is a recall check on the query set; how many had to be injected is the size of the gap.

- anchors already in the pull: **41**; injected: **41**
- free seeds already in the pull: **186**; injected: **289**
- **gold found by the query itself: 41/82** — this is the recall check that matters. The rest are present only because they were injected, and an injected anchor tests nothing about the query.
- free seeds unmatchable (no id and no DOI): 40

### Gold found by the query, by direction (scope §2)

| direction | found by query | gold |
|---|---|---|
| `BACKWARD` | 7 | 15 |
| `FORWARD` | 3 | 4 |
| `THEORY` | 3 | 8 |
| `UNKNOWN` | 28 | 55 |

## Abstracts

**692 of 3254** records carry no abstract. A title-only row cannot support scope §10's tags — `design-is-not-a-property-of-the-title` — so these are a separate screening problem and are flagged rather than treated as ordinary rows.

## Blinding

`quantity-quality-tradeoff-screen-universe.json` carries **no** gold flags and no provenance. The key is in `quantity-quality-tradeoff-screen-gold.json` and is merged only when scoring sensitivity. A screen that can see which rows are gold cannot measure its own (`a-positives-only-screen-cannot-measure-sensitivity`).
