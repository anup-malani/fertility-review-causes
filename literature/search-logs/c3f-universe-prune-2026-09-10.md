# C.3.f universe prune — 2026-09-10

By `source/build/goldset/387_c3f_universe_prune.py`.

**4,565 → 4,181** (384 removed).

Rule: a record is removed only if the ungated FLOW_MEASUREMENT channel is its *only*
provenance **and** `intergenerational wealth` is the *only* flow-accounting term it matches. A record
any other channel or term reaches is kept.

Screen ids were frozen before the prune, so the 50 verdicts already written
still address the same records. No anchor or decoy was removed.
